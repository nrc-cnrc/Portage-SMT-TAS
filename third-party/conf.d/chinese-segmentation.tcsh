############ Chinese segmenter ############
if (! $?CHINESE_SEGMENTATION_HOME_OVERRIDE) then
   set CHINESE_SEGMENTATION_HOME=$PORTAGE/third-party/chinese-segmentation
else
   set CHINESE_SEGMENTATION_HOME=$CHINESE_SEGMENTATION_HOME_OVERRIDE
endif

if ( -d $CHINESE_SEGMENTATION_HOME ) then
   setenv PATH $CHINESE_SEGMENTATION_HOME/bin:$PATH
endif

unset CHINESE_SEGMENTATION_HOME
